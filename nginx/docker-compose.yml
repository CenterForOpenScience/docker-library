nginx:
#  image: nginx
  build: .
#  command: [nginx-debug, '-g', 'daemon off;']
  volumes:
   - ./nginx.conf:/etc/nginx/nginx.conf
   - ./modsec_includes.conf:/usr/local/nginx/conf/modsec_includes.conf
   - ./modsecurity.conf:/usr/local/nginx/conf/modsecurity.conf
   - ./crs-setup.conf:/usr/local/nginx/conf/rules/crs-setup.conf
  environment:
   - NGINX_HOST=foobar.com
   - NGINX_PORT=80
  ports:
    - 8000:80
  expose:
    - 8000
